enum StateEnum { none, loading, succeed, empty, error }

enum TypeToast { normal, error, success }

enum PaginationState { empty, uninitialized, refreshing, initialFetching, fetching, fetched, noMoreData, error }

